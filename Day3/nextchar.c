#include<stdio.h>
#include<conio.h>
int main(){
    // char currentChar;
    // printf("Enter a character: ");
    // //scanf("%c", &currentChar);
    // char nextChar = currentChar + 1;
    // printf("The next character after %c is %c\n", currentChar, nextChar);

    char var=getchar();
    char nextchar=var +1;
    putchar(nextchar);

    char var1=getchar();
    char nextchar1=var1 -1;
    putchar(nextchar1);

}